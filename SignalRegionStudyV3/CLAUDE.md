# CLAUDE.md

The module guide for `SignalRegionStudyV3` is maintained in AGENTS.md:

@AGENTS.md

Key environment rule: always source the module-local setup from this directory
(not the repository root `setup.sh`) before running anything here:

```bash
cd SignalRegionStudyV3
source setup.sh
```
