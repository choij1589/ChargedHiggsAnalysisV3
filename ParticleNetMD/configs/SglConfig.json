{
  "description": "Mass-Decorrelated ParticleNet training configuration with DisCo loss",
  "version": "1.0",

  "training_parameters": {
    "train_folds": [0, 1, 2],
    "valid_folds": [3],
    "test_folds": [4],
    "max_epochs": 81,
    "batch_size": 512,
    "dropout_p": 0.4,
    "early_stopping_patience": 10,
    "loss_type": "disco",
    "balance_weights": true,
    "max_events_per_fold_per_class": 50000,
    "augment_phi_rotation": true,
    "comment": "DisCo loss for mass decorrelation. train_folds for training, valid_folds for early stopping, test_folds for final evaluation. augment_phi_rotation: apply random phi rotation to Px,Py during training."
  },

  "disco_parameters": {
    "disco_lambda": 0.05,
    "comment": "disco_lambda: weight for mass decorrelation (DisCo(score, mass1) + DisCo(score, mass2))"
  },

  "model_config": {
    "default_model": "ParticleNet",
    "nNodes": 256,
    "comment": "Node features: [E, Px, Py, Pz, charge, isMuon, isElectron, isJet, isBjet] (9 features)"
  },

  "optimization_config": {
    "optimizer": "Adam",
    "initLR": 0.0005,
    "weight_decay": 4e-05,
    "scheduler": "ExponentialLR",
    "comment": "Valid optimizers: Adam, RMSprop, Adadelta. Valid schedulers: StepLR, ExponentialLR, CyclicLR, ReduceLROnPlateau"
  },

  "background_config": {
    "mode": "groups",
    "background_groups": {
      "nonprompt": ["TTLL_powheg",
                    "TTLL_mtop171p5_powheg",
                    "TTLL_mtop173p5_powheg",
                    "TTLL_TuneCP5up_powheg",
                    "TTLL_TuneCP5down_powheg",
                    "TTLL_TuneCP5CR1_powheg",
                    "TTLL_TuneCP5CR2_powheg",
                    "TTLL_hdamp_up_powheg",
                    "TTLL_hdamp_down_powheg"
      ],
      "diboson": ["WZTo3LNu", "ZZTo4L_powheg"],
      "ttX": ["TTZ", "tZq"]
    },
    "backgrounds_list": [],
    "comment": "mode: 'groups' for physics-motivated grouping, 'individual' for each background as separate class."
  },

  "dataset_config": {
    "signal_prefix": "TTToHcToWAToMuMu-",
    "background_prefix": "Skim_TriLep_",
    "era_sample_mapping": {
      "WZTo3LNu": {"run2": "WZTo3LNu_amcatnlo", "run3": "WZTo3LNu_powheg"},
      "TTZ": {"run2": "TTZToLLNuNu", "run3": "TTZ_M50"}
    },
    "comment": "B-jets are separate particles (no btagScore). Dataset includes mass1, mass2 for decorrelation. era_sample_mapping: samples with different names across Run2/Run3 eras."
  },

  "system_config": {
    "device": "cuda",
    "debug": false,
    "comment": "device: 'cpu', 'cuda', or 'cuda:X' for specific GPU."
  },

  "output_config": {
    "results_dir": "LambdaSweep",
    "comment": "Output directory within ParticleNetMD/"
  }
}
